/**
 * @param {number} lat - Latitude center, in decimal, ex 71.0050
 * @param {number} long - Longitude center, in decimal, ex 40.000
 * @returns [POI] pointsOfInterest - All POI in that range
 */
function SearchPOIsInRadius(req, resp) {
  
  ClearBlade.init({request:req})

  
  

  

  // create box with geo
  // query less than , greater than
  // return yo
}
